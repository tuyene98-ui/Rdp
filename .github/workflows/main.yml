name: Windows RDP via Tailscale (120GB)

on:
  workflow_dispatch:

jobs:
  win-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Enable RDP
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                            -Name "fDenyTSConnections" -Value 0 -Force
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"

      - name: Create RDP User
        shell: pwsh
        run: |
          $Password = -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 14 | % {[char]$_})
          $Secure = ConvertTo-SecureString $Password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $Secure
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          echo "RDP_PASS=$Password" | Out-File -Append $env:GITHUB_ENV

      - name: Expand Disk to 120GB
        shell: pwsh
        run: |
          $vhd = (Get-WmiObject Win32_DiskDrive | Where-Object { $_.Model -like "*Google*" }).DeviceID
          $part = Get-Partition -DiskNumber 0 -PartitionNumber 3
          Resize-Partition -InputObject $part -Size 120GB

      - name: Install Tailscale
        run: |
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $file = "$env:TEMP\ts.msi"
          Invoke-WebRequest -Uri $url -OutFile $file
          Start-Process msiexec.exe -ArgumentList "/i `"$file`" /quiet /norestart" -Wait

      - name: Connect to Tailscale
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=GH-RDP-${{ github.run_id }}

          $tsIP = ""
          for ($i=0; $i -lt 20 -and !$tsIP; $i++) {
            $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
            Start-Sleep 3
          }

          echo "TAILSCALE_IP=$tsIP" | Out-File -Append $env:GITHUB_ENV

      - name: Keep Alive (GreenboyMC)
        run: |
          Write-Host "==========================================="
          Write-Host "     WINDOWS RDP IS NOW ONLINE (TAILSCALE)"
          Write-Host "==========================================="
          Write-Host "IP Address  : $env:TAILSCALE_IP"
          Write-Host "User        : RDP"
          Write-Host "Password    : $env:RDP_PASS"
          Write-Host "==========================================="
          while ($true) {
            Start-Sleep -Seconds 300
          }
